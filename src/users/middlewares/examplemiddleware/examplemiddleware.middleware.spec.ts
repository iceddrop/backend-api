import { ExamplemiddlewareMiddleware } from './examplemiddleware.middleware';

describe('ExamplemiddlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new ExamplemiddlewareMiddleware()).toBeDefined();
  });
});
